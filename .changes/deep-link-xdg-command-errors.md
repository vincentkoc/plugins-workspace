---
"deep-link": patch
"deep-link-js": patch
---

Return an I/O error when an XDG registration, unregistration, or query command exits unsuccessfully on Linux, while keeping desktop-database refresh optional during unregistration. Match the queried desktop filename exactly, remove only the app's entries from default and added associations, and preserve other schemes, applications, list order, and desktop command quoting.
